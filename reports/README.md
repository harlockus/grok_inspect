# Reports directory

Scan outputs are written here:

- `grok-inspect-<timestamp>.{md,json,html}`
- `latest.md` / `latest.json` / `latest.html`

**These files are gitignored.** They can contain host metadata (paths, process names, listeners). Do not commit them to GitHub.

Protect this folder the same way you protect other IR evidence.
