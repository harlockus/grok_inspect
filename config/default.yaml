model:
  id: grok-4.5
  reasoning_effort: high
  temperature: 0.2

scan:
  collector_timeout_sec: 45
  # Larger package for CISO-grade Grok briefs (still redacted)
  payload_max_chars: 350000


reports:
  dir: reports
