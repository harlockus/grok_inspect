# Grok Inspect — secrets live ONLY in a local copy of this file.
#
#   cp .env.example .env
#   # then set your real key below
#   grok-inspect doctor
#
# .env is gitignored. NEVER commit a real key.
# Get a key: https://console.x.ai/

XAI_API_KEY=

# Optional model override (default grok-4.5 — do not use mini/fast):
# XAI_MODEL=grok-4.5
